# Production Environment Configuration for VPS
NODE_ENV=production
PORT=5000

# PostgreSQL (via docker compose service)
# Replace user/password/db if you changed them in compose
DATABASE_URL=postgres://user:password@db:5432/auto_eval

# Security
# CHANGE THIS to a strong random string (64+ chars recommended)
JWT_SECRET=your-very-strong-production-jwt-secret

# CORS
# Replace with your VPS domain or IP (with http/https accordingly)
FRONTEND_URL=http://your-vps-domain-or-ip